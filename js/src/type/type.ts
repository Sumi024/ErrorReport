export interface ReporterConfig {
    ReportUrl: string,
    ReportMethod ?:string,
    ReportHeaders?: ReporterHeaders,
}

export interface ReporterHeaders {
    ReportUrl: string,
    ReportMethod ?:string,
    ReportHeaders?: ReporterHeaders,
}